{
  "name": "@wellbloom/api",
  "private": true,
  "scripts": {
    "start": "tsx src/main.ts",
    "start:dev": "tsx watch src/main.ts",
    "test": "tsx --tsconfig tsconfig.json --test test/payments.mpesa.spec.ts"
  },
  "dependencies": {
    "@nestjs/jwt": "^12.0.2",
    "@nestjs/schedule": "^12.0.2",
    "bcrypt": "^6.0.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.15.1"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/express": "^5.0.6"
  }
}
