{
  "name": "@wellbloom/api",
  "private": true,
  "scripts": {
    "start": "tsx src/main.ts",
    "start:dev": "tsx watch src/main.ts",
    "test": "tsx --tsconfig tsconfig.json --test test/*.spec.ts"
  },
  "dependencies": {
    "@nestjs/jwt": "^12.0.2",
    "@nestjs/schedule": "^12.0.2",
    "@nestjs/throttler": "^6.7.0",
    "bcrypt": "^6.0.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.15.1",
    "helmet": "^8.3.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/express": "^5.0.6"
  }
}
